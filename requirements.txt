# Tested with Python 3.12–3.13, torch 2.14, transformers 5.17, numpy 2.5
# On Colab/Kaggle, torch is preinstalled — keep it; don't downgrade.
torch>=2.4
transformers>=4.46
accelerate
sentence-transformers
faiss-cpu>=1.11
numpy
nvidia-ml-py>=12.535    # provides `pynvml`, incl. the total-energy counter
packaging
pandas
matplotlib
tabulate                # DataFrame.to_markdown in reports
pytest
aiohttp                 # streaming server (serve_engine.py) and HTTP benchmark client

# Experiment 5 only — install AFTER everything else (vLLM replaces torch):
#   pip install vllm aiohttp
